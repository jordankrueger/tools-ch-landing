---
interface Props {
  href: string;
  label: string;
  variant?: 'compact' | 'hero';
}
const { href, label, variant = 'compact' } = Astro.props;
const className = variant === 'hero' ? 'ch-cta ch-cta-hero' : 'ch-cta';
---
<a class={className} href={href}>
  {label} <span class="ch-cta-icon">→</span>
</a>
